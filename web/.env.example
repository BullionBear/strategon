# Human API base URL (ControlPlaneService). Defaults to http://127.0.0.1:8081
# After Discord/mock login the UI exchanges a one-time code for a Bearer token
# (stored in localStorage) so cross-origin calls work without a Vite proxy.
VITE_API_BASE=http://127.0.0.1:8081
